import { PressedDirective } from './pressed.directive';

describe('PressedDirective', () => {
  it('should create an instance', () => {
    const directive = new PressedDirective();
    expect(directive).toBeTruthy();
  });
});
