import { ChatDirective } from './chat.directive';

describe('ChatDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatDirective();
    expect(directive).toBeTruthy();
  });
});
