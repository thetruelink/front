import { OverDirective } from './over.directive';

describe('OverDirective', () => {
  it('should create an instance', () => {
    const directive = new OverDirective();
    expect(directive).toBeTruthy();
  });
});
